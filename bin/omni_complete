#/usr/bin/env bash
OMNIHOME=${HOME}/OmniStream
complete -W "$(ls ${OMNIHOME}/components)" o+
complete -W "$(ls ${OMNIHOME}/enabled)" o-
complete -W "start stop up down status stats restart services setup apps bin home logs mounts rebuild update clean pull help" omni
