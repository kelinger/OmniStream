#
# SABnzbd - Usenet Downloader
#
  sabnzbd:
    image: linuxserver/sabnzbd
    container_name: ${SABNZBDNAME}
    hostname: ${SABNZBDNAME}
    domainname: ${MYDOMAIN}
    restart: unless-stopped
    depends_on:
      - traefik
      - omnimount
    environment:
      - PGID=${GROUPID}
      - PUID=${USERID}
      - TZ=${TIMEZONE}
    labels:
      - traefik.enable=true
      - traefik.http.routers.sabnzbd.rule=Host(`${SABNZBDNAME}.${MYDOMAIN}`)
      - traefik.http.routers.sabnzbd.tls=true
      - traefik.http.routers.sabnzbd.tls.certresolver=lets-encrypt
      - traefik.port=8080
    volumes:
      - ${CONFIGS}/${SABNZBDNAME}:/config
      - ${MERGEMOUNT}:/cloud
      - ${LOGS}:/logs
      - ${CONFIGS}/healthchecks/sabnzbdcheck:/sabnzbdcheck
    networks:
      - OmniNet
    healthcheck:
      test: ["CMD-SHELL", "/sabnzbdcheck"]
      interval: 10s
      timeout: 2s
      retries: 3
